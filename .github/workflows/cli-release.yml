name: cli-release

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-latest
            target: x86_64-apple-darwin
    runs-on: ${{ matrix.os }}
    env:
      SHIORI_EXPORT_URL: ${{ secrets.SHIORI_EXPORT_URL }}
      SHIORI_OIDC_CLIENT_ID: ${{ secrets.SHIORI_OIDC_CLIENT_ID }}
      SHIORI_OIDC_CLIENT_SECRET: ${{ secrets.SHIORI_OIDC_CLIENT_SECRET }}
    steps:
      - uses: actions/checkout@v6
      - run: rustup target add ${{ matrix.target }}
      - run: cargo build --package cli --release --target ${{ matrix.target }}
      - run: tar -C target/${{ matrix.target }}/release -czf shiori-${{ matrix.target }}.tar.gz shiori
      - uses: actions/upload-artifact@v4
        with:
          name: shiori-${{ matrix.target }}
          path: shiori-${{ matrix.target }}.tar.gz

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: dist
      - env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create '${{ github.ref_name }}' \
            --repo '${{ github.repository }}' \
            --title '${{ github.ref_name }}' \
            dist/*
